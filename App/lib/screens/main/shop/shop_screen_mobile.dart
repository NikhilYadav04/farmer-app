import 'package:flutter/material.dart';

class ShopScreenMobile extends StatelessWidget {
  const ShopScreenMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Icon(Icons.shop),
      ),
    );
    
  }
}
